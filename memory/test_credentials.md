# Test Credentials — GymTrack Pro

Auth is handled by the **Blink SDK** (headless email/password), endpoint `https://blink.new/api/auth/*`, project `gymtrack-mobile-app-u6wmrz3v`. Accounts are created on demand from the **Cuenta** tab (Crear Cuenta). Password min length: 6.

## Demo account (created, works)
- Email: `demo@gymtrack.app`
- Password: `demo1234`

## Notes for testers
- Log in via bottom tab **CUENTA** → toggle "¿No tienes cuenta? Regístrate" for signup, or use the demo account to sign in.
- The **Calendario** tab requires being logged in to add/edit workouts.
- Any fresh `something@example.com` / `test1234` also works via signup.
